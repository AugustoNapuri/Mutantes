SET DATABASE SQL SIZE FALSE;
DROP TABLE IF EXISTS DNA_CASE;

CREATE TABLE DNA_CASE (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
   dna VARCHAR DEFAULT '' NOT NULL,
   is_mutant BOOLEAN,
   amount_mutant_sequences INT,
   CONSTRAINT pk_dnacase PRIMARY KEY (id)
);