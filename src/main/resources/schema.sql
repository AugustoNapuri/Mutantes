SET DATABASE SQL SIZE FALSE;
DROP TABLE IF EXISTS ADN_CASE;

CREATE TABLE ADN_CASE (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
   adn VARCHAR DEFAULT '' NOT NULL,
   is_mutant BOOLEAN,
   mutant_sequences INT,
   CONSTRAINT pk_adncase PRIMARY KEY (id)
);